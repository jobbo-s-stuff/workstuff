def main():
    pass
    #csv_reader.convert()
    #merge date/time
    #take min/max timestamps for endTime (/reservations)
    #connect.db_query()
    #wunder.get_plates() (loop through db_query results to get plates for carids)
    #match plates from get_plates() with plates from convert() & check against timestamps
    #wunder.post_vouchers and pass strings

if __name__ == '__main__':
    main()